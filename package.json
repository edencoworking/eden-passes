{
  "name": "eden-passes",
  "version": "1.2.1",
  "private": true,
  "description": "Frontend-only React application for managing coworking passes and customers (mock localStorage data).",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --watchAll=false",
    "lint": "eslint src --ext .js,.jsx",
    "format": "prettier --write ."
  },
  "dependencies": {
    // (unchanged; keep existing dependencies exactly as they currently are)
  },
  "devDependencies": {
    // (unchanged; keep existing devDependencies exactly as they currently are)
  }
}
